server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/pokemon_prophunt?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: pokemon
    password: pokemon1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  jackson:
    serialization:
      write-dates-as-timestamps: false

cors:
  allowed-origins: http://localhost:5173,http://topon.dev,https://topon.dev
